{
	"indent": 4,
	"remove": ["devDependencies", "dependencies", "lint-staged", "bin", "module"],
	"replace": {
		"main": "index.mjs",
		"scripts": {
			"npm:postpack": "clean-package restore"
		}
	}
}
